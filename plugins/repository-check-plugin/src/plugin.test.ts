import { repositoryCheckPluginPlugin } from './plugin';

describe('repository-check-plugin', () => {
  it('should export plugin', () => {
    expect(repositoryCheckPluginPlugin).toBeDefined();
  });
});
